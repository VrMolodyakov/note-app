export const style = {
    control: (base: any) => ({
        ...base,
        boxShadow: "none", 
        borderColor: "#cccccc",
        "&:hover": {
            borderColor: "#cccccc"
        }
    })
};